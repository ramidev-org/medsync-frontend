/* eslint-disable no-console */

// Kept for compatibility with the default Expo template script name.
// This repo has real app code under `app/`, so a destructive reset would be risky.
// If you want a clean slate, prefer: `git clean -xfd` (careful) or delete `.expo/` + `dist/`.

console.log("[reset-project] No-op: project reset is disabled in this repo.");
